/**
 * App shell placeholder. The real UI — Today command center, split work surface, 3-pane
 * fallback — lands in Phase 7 (7a/7b/7c) with Tailwind + shadcn/ui + Lucide.
 */
export function App() {
  return <main>Mailordomo</main>;
}
