{
  "name": "@mailordomo/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsup src/index.ts --format esm --clean --silent",
    "verify-mailbox": "node scripts/verify-mailbox.ts"
  },
  "devDependencies": {
    "@mailordomo/server": "*",
    "@types/better-sqlite3": "^7.6.13",
    "@types/mailparser": "^3.4.6",
    "@types/nodemailer": "^8.0.0",
    "eslint": "*",
    "vitest": "*"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.4",
    "@mailordomo/shared": "*",
    "better-sqlite3": "^12.10.0",
    "hono": "^4.12.23",
    "imapflow": "^1.3.6",
    "mailparser": "^3.9.9",
    "nodemailer": "^8.0.10"
  }
}
